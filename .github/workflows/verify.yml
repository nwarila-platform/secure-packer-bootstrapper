name: Verify

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: verify-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify-ubuntu:
    name: Verify (${{ matrix.runner }})
    strategy:
      fail-fast: false
      matrix:
        runner:
          - ubuntu-22.04
          - ubuntu-24.04
    permissions:
      contents: read
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y openssh-client openssl shellcheck

      - name: Verify
        run: bash scripts/verify.sh

  verify-rocky:
    name: Verify (rockylinux-${{ matrix.version }})
    strategy:
      fail-fast: false
      matrix:
        version:
          - '8'
          - '9'
          - '10'
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    container:
      image: rockylinux/rockylinux:${{ matrix.version }}
    steps:
      - name: Install Dependencies
        run: |
          dnf install -y git openssh-clients openssl

      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Verify
        run: bash scripts/verify.sh
